diff -ruN a/.github/workflows/ci.yml b/.github/workflows/ci.yml
--- a/.github/workflows/ci.yml	2026-09-19 06:53:32.115698881 +0000
+++ b/.github/workflows/ci.yml	2026-09-19 06:53:32.116596156 +0000
@@ -8,7 +8,7 @@
 
 jobs:
   build:
-    runs-on: ubuntu-latest
+    runs-on: ubuntu-24.04
     services:
       postgres:
         image: postgres:18
@@ -31,10 +31,10 @@
 
     steps:
       - name: Checkout code
-        uses: actions/checkout@v4
+        uses: actions/checkout@v5
 
       - name: Setup Node.js
-        uses: actions/setup-node@v4
+        uses: actions/setup-node@v5
         with:
           node-version: 22
           cache: "npm"
diff -ruN a/app/globals.css b/app/globals.css
--- a/app/globals.css	2026-09-19 06:53:32.111237140 +0000
+++ b/app/globals.css	2026-09-19 06:53:32.112206228 +0000
@@ -8,8 +8,6 @@
   --color-background: var(--background);
   --color-foreground: var(--foreground);
   --font-sans: var(--font-sans);
-  --font-display: var(--font-display);
-  --font-hand: var(--font-hand);
   --font-mono: var(--font-geist-mono);
   --font-heading: var(--font-display);
   --color-sidebar-ring: var(--sidebar-ring);
@@ -53,8 +51,8 @@
   --radius-2xl: calc(var(--radius) * 1.8);
   --radius-3xl: calc(var(--radius) * 2.2);
   --radius-4xl: calc(var(--radius) * 2.6);
-  --font-display: "Bodoni Moda", serif;
-  --font-hand: "Alex Brush", cursive;
+  --font-display: var(--font-bodoni), "Bodoni Moda", serif;
+  --font-hand: var(--font-alex-brush), "Alex Brush", cursive;
   
 }
 
diff -ruN a/app/layout.tsx b/app/layout.tsx
--- a/app/layout.tsx	2026-09-19 06:53:32.106232835 +0000
+++ b/app/layout.tsx	2026-09-19 06:53:32.107820381 +0000
@@ -1,11 +1,15 @@
 import { Toaster } from "sonner";
 import "./globals.css";
-import { Geist } from "next/font/google";
+import { Geist, Bodoni_Moda, Alex_Brush } from "next/font/google";
 import { cn } from "@/lib/utils";
 import type { Metadata, Viewport } from "next";
 import { AnalyticsConsent } from "@/components/privacy/analytics-consent";
 
 const geist = Geist({ subsets: ["latin"], variable: "--font-sans" });
+// Bodoni Moda: elegant serif similar to Bodoni/Didot
+const bodoni = Bodoni_Moda({ subsets: ["latin"], style: ["normal", "italic"], axes: ["opsz"], display: "swap", variable: "--font-bodoni" });
+// Alex Brush: ornate script similar to Bickham Script Pro
+const alexBrush = Alex_Brush({ subsets: ["latin"], weight: "400", display: "swap", variable: "--font-alex-brush" });
 
 export const metadata: Metadata = {
   metadataBase: new URL((process.env.NEXT_PUBLIC_APP_URL || "http://localhost:3000")),
@@ -35,15 +39,7 @@
 
 export default function RootLayout({ children }: { children: React.ReactNode }) {
   return (
-    <html lang="en" className={cn("dark font-sans", geist.variable)}>
-      <head>
-        <link rel="preconnect" href="https://fonts.googleapis.com" />
-        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="" />
-        {/* Bodoni Moda - elegant serif similar to Bodoni/Didot */}
-        <link href="https://fonts.googleapis.com/css2?family=Bodoni+Moda:ital,opsz,wght@0,6..96,400;0,6..96,500;0,6..96,600;0,6..96,700;1,6..96,400;1,6..96,500&display=swap" rel="stylesheet" />
-        {/* Alex Brush - ornate script similar to Bickham Script Pro */}
-        <link href="https://fonts.googleapis.com/css2?family=Alex+Brush&display=swap" rel="stylesheet" />
-      </head>
+    <html lang="en" className={cn("dark font-sans", geist.variable, bodoni.variable, alexBrush.variable)}>
       <body>
         {children}
         <Toaster theme="dark" position="top-right" closeButton richColors
